import React from "react"
import Layout from "../components/Layout"

export default ({
    name,
    age
}) => {



    return (
        <Layout>
            <h1>{name}</h1>
            <p>{name}</p>
        </Layout>
    )
}